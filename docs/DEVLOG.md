# Devlog

## log 1
buildt 2 difrent systems, one clones a project template and has commands like create project, compile project, run project, set build commands, clear, exit.
the other is the actual project template, for now its just a basic OpenGL window. 

## log 2 
started work on the actual engine so far its just some basic rendering, features i have created so far are LOD system, frustum culling, Octree data structure, and inctence rendering / batch. also creted an optional fly cam module and an input mannager. started on a more detailed doc page: [View](DOCUMENTATION.md)